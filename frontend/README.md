# Frontend React App

This is a simple React frontend for the AI Interview Assistant.

## Setup

1. Make sure you have Node.js installed.
2. Run `npm install` to install dependencies.
3. Run `npm run dev` to start the development server.

The app uses `fetch` to communicate with the FastAPI backend running on `http://127.0.0.1:8000`.
